server {
    listen 8080;
    
    # Error 1: autoindex is only registered inside locations in your parser hpp
    autoindex on; 

    location / {
        root /var/www;
        
        # Error 2: server_name belongs exclusively to ServerBlock scope
        server_name localhost; 
    }
}